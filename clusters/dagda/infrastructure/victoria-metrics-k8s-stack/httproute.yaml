apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana
  namespace: monitoring
spec:
  parentRefs:
    - name: eg
      namespace: envoy-gateway-system
  hostnames:
    - grafana.aiectomy.xyz
  rules:
    - backendRefs:
        - name: monitoring-victoria-metrics-k8s-stack-grafana
          port: 80
