apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: plex
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: plex
  digestReflectionPolicy: IfNotPresent
  policy:
    semver:
      range: ">=1.43.3"
