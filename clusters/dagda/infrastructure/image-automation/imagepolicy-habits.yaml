apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: habits
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: habits
  digestReflectionPolicy: IfNotPresent
  policy:
    semver:
      range: ">=0.1.0"
