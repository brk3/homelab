apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: movieswipe
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: movieswipe
  digestReflectionPolicy: IfNotPresent
  policy:
    semver:
      range: ">=0.1.0"
