apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: forgejo-runner
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: forgejo-runner
  digestReflectionPolicy: IfNotPresent
  policy:
    semver:
      range: ">=13.0.0 <14.0.0"
