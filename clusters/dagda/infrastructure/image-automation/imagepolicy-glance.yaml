apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: glance
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: glance
  digestReflectionPolicy: IfNotPresent
  policy:
    semver:
      range: ">=0.8.5"
